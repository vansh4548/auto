/* Container for the image on mobile */
.pbmit-col-stretched-left-mobile {
  position: relative;
  height: 350px; /* Reduced from 400px to look better on phones */
  width: 100%;
  border-radius: 30px;
  overflow: hidden;
  background-size: cover;
  background-position: center;
}

/* Shinking the yellow section for mobile */
@media (max-width: 767px) {
  .pbmit-ihbox-style-3 {
    padding: 15px !important; /* Smaller internal padding */
    margin: 10px; /* Slight offset from image edge */
    border-radius: 20px !important; /* Softer corners for small box */
    display: inline-block; /* Prevent it from taking full width */
    max-width: 85%;
  }

  .pbmit-ihbox-contents .pbmit-element-title {
    font-size: 16px !important;
    line-height: 1.2 !important;
  }

  /* Make the white icon circle smaller */
  .pbmit-ihbox-icon-wrapper {
    width: 45px !important;
    height: 45px !important;
    font-size: 20px !important;
  }
}

/* Original Laptop Layout */
@media (min-width: 1200px) {
  .xl\:pbmit-col-stretched-left {
    position: absolute;
    height: 100%;
    top: 0;
    left: 0;
    right: 0;
    width: auto;
    z-index: 1;
    border-radius: 30px;
  }
}
.pbmit-heading-subheading {
    margin-bottom: 50px;
    position: relative;
    z-index: 1
}

.pbmit-heading-subheading .pbmit-subtitle {
    font-size: 13px;
    line-height: 24px;
    letter-spacing: .5px;
    text-transform: uppercase;
    font-style: normal;
    position: relative;
    display: inline-block;
    color: var(--pbmit-global-color)
}

.pbmit-heading-subheading .pbmit-subtitle:after,
.pbmit-heading-subheading .pbmit-subtitle:before {
    content: '/';
    display: inline-block
}

.pbmit-heading-subheading .pbmit-title {
    font-size: 48px;
    line-height: 50px;
    letter-spacing: -2px;
    text-transform: none;
    font-style: normal;
    margin-bottom: 0
}

@keyframes anime {
    0% {
        transform-origin: left;
        transform: scaleX(0)
    }

    50% {
        transform-origin: left;
        transform: scaleX(1)
    }

    51% {
        transform-origin: right
    }

    100% {
        transform-origin: right;
        transform: scaleX(0)
    }
}

@keyframes anime1 {
    0% {
        transform-origin: top;
        transform: scaleY(0)
    }

    50% {
        transform-origin: top;
        transform: scaleY(1)
    }

    51% {
        transform-origin: bottom
    }

    100% {
        transform-origin: bottom;
        transform: scaleY(0)
    }
}

.pbmit-animation-style1,
.pbmit-animation-style2 {
    display: inline-block;
    position: relative;
    overflow: hidden;
    transition: background .3s, border .3s, border-radius .3s, box-shadow .3s, transform
}

.pbmit-animation-style1 img,
.pbmit-animation-style2 img {
    transform-origin: 50% 50%;
    transition: 2s cubic-bezier(.5, .5, 0, 1)
}

.pbmit-static-box-style-1 {
    margin-bottom: 30px
}

.pbmit-static-box-style-1 .pbmit-img-wrapper {
    position: relative;
    overflow: hidden;
    border-radius: 30px;
    margin-bottom: 15px
}

.pbmit-static-box-style-1 .pbminfotech-post-item {
    text-align: center
}

.pbmit-static-box-style-1 .pbmit-img-wrapper img {
    width: 100%;
    border-radius: 30px;
    -webkit-transition: all .3s ease-in-out;
    -ms-transition: all .3s ease-in-out;
    -o-transition: all .3s ease-in-out;
    transition: all .3s ease-in-out
}

.pbmit-static-box-style-1:hover .pbmit-img-wrapper img {
    -webkit-transform: scale(1.05);
    -ms-transform: scale(1.05);
    transform: scale(1.05)
}

.pbmit-static-box-style-1 .pbmit-title-wrapper {
    position: absolute;
    left: 0;
    right: 0;
    margin: 0 auto;
    bottom: 0;
    text-align: center;
    display: inline-table;
    padding: 20px 25px 5px 25px;
    border-radius: 25px 25px 0 0;
    background-color: var(--pbmit-white-color)
}

.pbmit-static-box-style-1 .pbmit-title-wrapper::after,
.pbmit-static-box-style-1 .pbmit-title-wrapper::before {
    content: "";
    position: absolute;
    background-color: transparent;
    bottom: 0;
    height: 40px;
    width: 20px;
    box-shadow: 0 20px 0 0 var(--pbmit-white-color)
}

.pbmit-static-box-style-1 .pbmit-title-wrapper::before {
    right: 100%;
    border-bottom-right-radius: 20px
}

.pbmit-static-box-style-1 .pbmit-title-wrapper::after {
    left: 100%;
    border-bottom-left-radius: 20px
}

.pbmit-static-box-style-1 .pbmit-title-inner {
    display: flex;
    align-items: center
}

.pbmit-static-box-style-1 .pbmit-staticbox-title {
    font-size: 20px;
    line-height: 24px;
    margin-bottom: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical
}

.pbmit-static-box-style-1 .pbmit-icon-wrapper {
    font-size: 60px;
    line-height: 60px;
    padding-bottom: 10px;
    color: var(--pbmit-global-color)
}

.pbmit-static-box-style-1 .pbmit-staticbox-title a .pbmit-button-icon-wrapper {
    display: none
}

.pbmit-static-box-style-2 {
    -webkit-transition: all .6s ease-in-out;
    -ms-transition: all .6s ease-in-out;
    -o-transition: all .6s ease-in-out;
    transition: all .6s ease-in-out
}

.pbmit-static-box-style-2 .pbmit-staticbox-title a .pbmit-button-icon-wrapper {
    display: none
}

.pbmit-static-box-style-2 .pbmit-staticbox-post-item {
    padding: 40px;
    border-radius: 30px;
    display: flex;
    width: 100%;
    height: 100%;
    justify-content: space-between;
    border: 1px solid rgba(var(--pbmit-blackish-color-rgb), .15)
}

.pbmit-static-box-style-2 .pbmit-icon-wrapper {
    font-size: 60px;
    line-height: 60px;
    padding-bottom: 20px;
    color: var(--pbmit-global-color)
}

.pbmit-static-box-style-2 .pbmit-staticbox-title {
    font-size: 24px;
    line-height: 28px;
    margin: 0
}

.pbmit-static-box-style-2 {
    flex: 1;
    display: block;
    transition: .7s;
    position: relative;
    overflow: hidden
}

.pbmit-static-box-style-2 .pbmit-img-text .pbmit-staticbox-title {
    align-self: flex-end;
    left: 50%;
    right: 50%;
    position: relative;
    writing-mode: vertical-lr;
    inline-size: 250px;
    overflow-wrap: break-word;
    -khtml-transform: translateX(-50%) rotate(180deg);
    -moz-transform: translateX(-50%) rotate(180deg);
    -ms-transform: translateX(-50%) rotate(180deg);
    -o-transform: translateX(-50%) rotate(180deg);
    transform: translateX(-50%) rotate(180deg)
}

.pbmit-static-box-style-2 .pbmit-img-text {
    display: grid;
    left: 50%;
    position: relative;
    transform: translateX(-50%)
}

@keyframes pbmit-faded {
    0% {
        opacity: 0
    }

    100% {
        opacity: 1
    }
}

@supports (mix-blend-mode:exclusion) {

    .pbmit-cursor.-exclusion,
    .pbmit-cursor.-opaque {
        mix-blend-mode: exclusion
    }
}

@supports (mix-blend-mode:exclusion) {

    .pbmit-cursor.-exclusion:before,
    .pbmit-cursor.-opaque:before {
        background: #fff
    }
}

@keyframes fadeIn {
    0% {
        opacity: 0
    }

    100% {
        opacity: 1
    }
}

.pbmit-text-editor {
    font-size: 16px;
    font-weight: 400;
    line-height: 1.6;
}

/* Mobile Optimizations for About Section */
@media (max-width: 767px) {
  .about-sec-two .pbmit-ihbox-style-3 {
    border-radius: 20px; /* Softer corners on mobile */
    padding: 1.5rem !important;
    width: 100%;
  }
  
  .about-sec-two .pbmit-heading-subheading h2 {
    font-size: 24px !important;
    line-height: 1.3 !important;
  }

  .about-sec-two .pbmit-btn {
    width: 100%; /* Full width button on mobile for easier tapping */
    text-align: center;
    justify-content: center;
  }
}

/* Fix for the SVG corners rotating correctly */
.pbmit-bottom-left-corner {
  transform: rotate(0deg);
}
.pbmit-top-right-corner {
  transform: rotate(0deg);
}

/* Animation refinement */
.about-sec-two .pbmit-ihbox-style-4:hover {
  transform: translateY(-5px);
  box-shadow: 0 10px 30px rgba(0,0,0,0.05);
}

